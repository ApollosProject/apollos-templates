import React from 'react';
import { storiesOf } from '@apollosproject/ui-storybook';

import ActionListFeature from '.';

storiesOf('ActionListFeature', module).add('default', () => (
  <ActionListFeature />
));
