#!/usr/bin/env node
"use strict";
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : new P(function (resolve) { resolve(result.value); }).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
var __generator = (this && this.__generator) || function (thisArg, body) {
    var _ = { label: 0, sent: function() { if (t[0] & 1) throw t[1]; return t[1]; }, trys: [], ops: [] }, f, y, t, g;
    return g = { next: verb(0), "throw": verb(1), "return": verb(2) }, typeof Symbol === "function" && (g[Symbol.iterator] = function() { return this; }), g;
    function verb(n) { return function (v) { return step([n, v]); }; }
    function step(op) {
        if (f) throw new TypeError("Generator is already executing.");
        while (_) try {
            if (f = 1, y && (t = op[0] & 2 ? y["return"] : op[0] ? y["throw"] || ((t = y["return"]) && t.call(y), 0) : y.next) && !(t = t.call(y, op[1])).done) return t;
            if (y = 0, t) op = [op[0] & 2, t.value];
            switch (op[0]) {
                case 0: case 1: t = op; break;
                case 4: _.label++; return { value: op[1], done: false };
                case 5: _.label++; y = op[1]; op = [0]; continue;
                case 7: op = _.ops.pop(); _.trys.pop(); continue;
                default:
                    if (!(t = _.trys, t = t.length > 0 && t[t.length - 1]) && (op[0] === 6 || op[0] === 2)) { _ = 0; continue; }
                    if (op[0] === 3 && (!t || (op[1] > t[0] && op[1] < t[3]))) { _.label = op[1]; break; }
                    if (op[0] === 6 && _.label < t[1]) { _.label = t[1]; t = op; break; }
                    if (t && _.label < t[2]) { _.label = t[2]; _.ops.push(op); break; }
                    if (t[2]) _.ops.pop();
                    _.trys.pop(); continue;
            }
            op = body.call(thisArg, _);
        } catch (e) { op = [6, e]; y = 0; } finally { f = t = 0; }
        if (op[0] & 5) throw op[1]; return { value: op[0] ? op[1] : void 0, done: true };
    }
};
Object.defineProperty(exports, "__esModule", { value: true });
var node_fetch_1 = require("node-fetch");
var introspectionQuery_1 = require("graphql/utilities/introspectionQuery");
var buildClientSchema_1 = require("graphql/utilities/buildClientSchema");
var schemaPrinter_1 = require("graphql/utilities/schemaPrinter");
var minimist = require("minimist");
var chalk_1 = require("chalk");
var version = require('../package.json').version;
var usage = "  Usage: get-graphql-schema ENDPOINT_URL > schema.graphql\n\n  " + chalk_1.default.bold('Fetch and print the GraphQL schema from a GraphQL HTTP endpoint') + "\n  (Outputs schema in IDL syntax by default)\n\n  Options:\n    --header, -h    Add a custom header (ex. 'X-API-KEY=ABC123'), can be used multiple times\n    --json, -j      Output in JSON format (based on introspection query)\n    --version, -v   Print version of get-graphql-schema\n";
function main() {
    return __awaiter(this, void 0, void 0, function () {
        var argv, endpoint, defaultHeaders, headers, response, _a, data, errors, schema;
        return __generator(this, function (_b) {
            switch (_b.label) {
                case 0:
                    argv = minimist(process.argv.slice(2));
                    if (argv._.length < 1) {
                        console.log(usage);
                        return [2 /*return*/];
                    }
                    if (argv['version'] || argv['v']) {
                        console.log(version);
                        process.exit(0);
                    }
                    endpoint = argv._[0];
                    defaultHeaders = {
                        'Content-Type': 'application/json',
                    };
                    headers = toArray(argv['header'])
                        .concat(toArray(argv['h']))
                        .reduce(function (obj, header) {
                        var _a = header.split('='), key = _a[0], value = _a[1];
                        obj[key] = value;
                        return obj;
                    }, defaultHeaders);
                    return [4 /*yield*/, node_fetch_1.default(endpoint, {
                            method: 'POST',
                            headers: headers,
                            body: JSON.stringify({ query: introspectionQuery_1.introspectionQuery }),
                        })];
                case 1:
                    response = _b.sent();
                    return [4 /*yield*/, response.json()];
                case 2:
                    _a = _b.sent(), data = _a.data, errors = _a.errors;
                    if (errors) {
                        throw new Error(JSON.stringify(errors, null, 2));
                    }
                    if (argv['j'] || argv['json']) {
                        console.log(JSON.stringify(data, null, 2));
                    }
                    else {
                        schema = buildClientSchema_1.buildClientSchema(data);
                        console.log(schemaPrinter_1.printSchema(schema));
                    }
                    return [2 /*return*/];
            }
        });
    });
}
function toArray(value) {
    if (value === void 0) { value = []; }
    return Array.isArray(value) ? value : [value];
}
main().catch(function (e) { return console.error(e); });
//# sourceMappingURL=index.js.map